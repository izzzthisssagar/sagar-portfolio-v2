# Fingerprints of gitleaks findings reviewed and confirmed as false positives.
# Format: <commit>:<file>:<rule-id>:<start-line> (see `gitleaks git` JSON report `Fingerprint`
# field). Every entry here must be justified in a comment — this file is not a place to hide a
# real secret, it's a record of confirmed non-secrets that would otherwise cause repeat noise.
#
# Reviewed 2026-08-04 (Phase 19, production-readiness sprint):

# .env.staging.example line 16: MINIO_ROOT_PASSWORD=replace-me-min-8-chars — a literal,
# documented placeholder in an *.example file (gitignored .env.staging is what's actually used,
# with real generated secrets, per scripts/staging.sh). Not a secret.
ec89a6ec5f5363a16a0263178101f18c517d0dc4:.env.staging.example:generic-api-key:16

# apps/api/src/auth/password-policy.ts lines 7-8: an entry in the COMMON_PASSWORDS denylist array
# used to *reject* weak passwords at signup/admin-create time. It is a list of known-bad
# passwords, not a credential for this app. Appears at two commits because the file was touched
# twice during Sprint 2.
95d528ec427f8ca77289883870d4d59790ac566f:apps/api/src/auth/password-policy.ts:generic-api-key:7
db1403f93d95291aa2ee1af58848478965a76158:apps/api/src/auth/password-policy.ts:generic-api-key:7
