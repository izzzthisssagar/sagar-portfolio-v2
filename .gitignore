node_modules/
.next/
dist/
coverage/
.turbo/
.env
.env.*
!.env.example
!.env.staging.example
playwright-report/
test-results/
lighthouse-results/
*.log
.DS_Store
# Local media storage adapter default output (MEDIA_STORAGE_LOCAL_PATH) — dev/test uploads only.
apps/api/.data/
# pg_dump output (scripts/db-backup.sh) — real database backups must never be committed.
backups/
